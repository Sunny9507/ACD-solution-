#include <bits/stdc++.h>
using namespace std;
int main() {
    int t; cin >> t;
    while(t--){
        string s; cin >> s;
        cout<<(s.substr(0, s.size()/2) == s.substr(s.size()/2,s.size()) ?"YES\n":"NO\n");
    }
    return 0;
}
